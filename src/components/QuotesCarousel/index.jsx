import React from "react";
import "./styles.css";
import quotes from "../../data/quotes";

const Carousel = ({ data }) => {
    return (<div className="Carousel-container">
        {'test'}
        {/* {
            data.map((item, index) => {
                return <div key={`carousal-${item.quote}`}>{item.quote}</div>
            })
        } */}
    </div>

    )
}

export default Carousel